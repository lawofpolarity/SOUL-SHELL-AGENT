# Wheel placement and operator audit — candidate, not canonical

This audit leaves the original FPVF/Soul-Shell README unchanged and does not change repository visibility.

## Placement review
The source describes FPVF kernels, a Soul-Shell composition S(n), nine integration points (IP1–IP9), system clusters and reference code. Integration points and clusters are **not automatically** the twelve perimeter nodes. The README references numbered nodes and other indexed LightMathematics elements, but does not establish a complete, independently verified N01–N12 placement and six reciprocal pairings for this repository. Preserve the source's own cross-references until its governing wheel specification is identified. Centre definition, group assignment, X/Y/Z placement, polarity, elemental classification and reciprocal mapping remain pending source audit.

## Mathematical reconciliation
The source uses `S(n)=I(n)+M_phi(n)+TPL(n)+Delta_RSCS(n)+R_emptyset`, `FPVF(n)=RSCS(n)*Phi_perspective(n)`, and `C_unified(n)=S(n)+FPVF(n)` as distinct expressions; do not conflate the shell, field and unified composition into a single centre without an explicit authoritative selection.

The README's IP5 uses `L_a=T_input/T_reaction`, a ratio; the candidate canonical processing lag is `T_processing/N_c`, a time per transformation. Preserve the source ratio as a **separately named/versioned source metric**, not an interchangeable canonical lag. Several expressions in the README have duplicated formatting; preserve original text and prepare any clean typesetting as a separately reviewed revision.

## Operator and admission status
The README does not provide a qualified twelve-node deviation vector, concurrent paths, overhead normalization, processing measurements, RFC fusion set, corpus-wide uniqueness result or DRSC closure certificate. Canonical RSCS, PTE, lag, SS, RFC and DRSC: **uncomputed**. Historical placement: pending; numerical qualification: pending; uniqueness: unresolved; DRSC admission: pending. Status: candidate, admitted count 0. No runtime authorization.
